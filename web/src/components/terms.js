import React from "react";

var d = React.DOM;

export class Terms extends React.Component {
    render() {
        return d.div(
            {className: "page", id: "terms"},

            "TODO"
        );
    }
}

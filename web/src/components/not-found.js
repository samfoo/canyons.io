import React from "react";

var d = React.DOM;

export default class NotFound extends React.Component {
    render() {
        return d.div(
            {},
            "not found"
        );
    }
}
